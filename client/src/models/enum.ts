export enum ToaletType {
  RoomShared = 0,
  FloorShared = 1,
  RoomBased = 2,
}

export enum ApplicationType {
  Budget = 1,
  SelfFinancing = 2,
  Disability = 3,
  SensitiveGroups = 4,
}

export enum ApplicationStatus {
  Review = 0,
  Accepted = 1,
  Denied = 2,
  Pending = 3,
}
